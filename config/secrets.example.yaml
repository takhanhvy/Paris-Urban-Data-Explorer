# Copier ce fichier en secrets.yaml et remplir les vraies valeurs.
# NE JAMAIS versionner secrets.yaml (il est dans .gitignore).

postgres:
  password: "CHANGE_ME"

airparif:
  api_key: "CHANGE_ME"

api:
  secret_key: "CHANGE_ME"   # clé de signature si JWT activé
